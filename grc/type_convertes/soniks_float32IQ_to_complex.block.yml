id: float32IQ_to_complex
label: Float (IQ) to Complex
category: '[Soniks]/Type Convertes'

inputs:
-   label: in
    dtype: float
    vlen: 1

outputs:
-   label: out
    dtype: complex
    vlen: 1

templates:
    imports: import soniks.type_convertes
    make: soniks.type_convertes.float32IQ_to_complex()

file_format: 1
