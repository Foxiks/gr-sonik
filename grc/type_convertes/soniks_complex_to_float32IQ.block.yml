id: complex_to_float32IQ
label: Complex to Float (IQ)
category: '[Soniks]/Type Convertes'

inputs:
-   label: in
    dtype: complex
    vlen: 1

outputs:
-   label: out
    dtype: float
    vlen: 1

templates:
    imports: import soniks.type_convertes
    make: soniks.type_convertes.complex_to_float32IQ()

file_format: 1
