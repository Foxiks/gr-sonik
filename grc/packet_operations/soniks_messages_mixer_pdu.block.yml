id: soniks_messages_mixer_pdu
label: Soniks Messages Mixer (PDU)
category: '[Soniks]/Packet Operations'

inputs:
-   domain: message
    id: in 0
-   domain: message
    id: in 1

outputs:
-   domain: message
    id: out

templates:
    imports: import soniks.packet_operations
    make: soniks.packet_operations.soniks_messages_mixer_pdu()

file_format: 1
