id: stratosat_xband_scrambler
label: Startosat X-Band Scrambler
category: '[Soniks]/Scramblers'

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import soniks.scramblers
    make: soniks.scramblers.stratosat_xband_scrambler()

file_format: 1
