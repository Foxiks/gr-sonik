id: stratosat_xband_scrambler
label: Startosat X-Band Scrambler
category: '[Sonik]/Scramblers'

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import sonik.scramblers
    make: sonik.scramblers.stratosat_xband_scrambler()

file_format: 1
